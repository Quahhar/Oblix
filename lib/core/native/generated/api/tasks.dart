// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0-beta.5.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';


            // These functions are ignored because they are not marked as `pub`: `add_months`, `arrange`, `child_progress`, `days_in_month`, `emit`, `focus_label`, `from_naive`, `is_overdue`, `next_weekly`, `rank_clock`, `rank_lateness`, `rank_priority`, `sanitized`, `sort_bucket`, `step_once`, `to_naive`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`


            /// Render a rule to the compact text stored in the `recurrence` column.
///
/// The shape is RRULE-shaped on purpose — `FREQ=WEEKLY;INTERVAL=2;BYDAY=MO,TH`
/// — so it stays readable in a database row and in a sync payload, and so a
/// later iCalendar export has somewhere obvious to start. It is not full
/// RFC 5545: only what the app can honestly honour is representable.
String  serializeRecurrence({required RecurrenceRule rule }) => RustLib.instance.api.crateApiTasksSerializeRecurrence(rule: rule);

/// Read a stored rule back. Unknown keys are ignored and a malformed rule is
/// `None` rather than an error, because a row written by a newer client must
/// degrade to "does not repeat" instead of breaking the list.
RecurrenceRule?  parseRecurrence({required String text }) => RustLib.instance.api.crateApiTasksParseRecurrence(text: text);

/// One-line human description for the row and the editor ("Every 2 weeks on
/// Mon, Thu"). Kept here so the phrasing cannot drift between screens.
String  describeRecurrence({required RecurrenceRule rule }) => RustLib.instance.api.crateApiTasksDescribeRecurrence(rule: rule);

/// The date a repeating task moves to once the current occurrence is done.
///
/// `from` is the occurrence being advanced past — the old due date under
/// [`RecurrenceMode::Schedule`], the completion date under
/// [`RecurrenceMode::Completion`]. The result is always strictly later than
/// `from`, so ticking an overdue chore can never leave it in the past: a
/// monthly bill three months late jumps to the next month that has not
/// happened yet rather than to a date that is still overdue. Pass `not_before`
/// (normally today) to enable that catch-up; pass `from` itself to disable it.
CivilDate?  nextOccurrence({required RecurrenceRule rule , required CivilDate from , required CivilDate notBefore }) => RustLib.instance.api.crateApiTasksNextOccurrence(rule: rule, from: from, notBefore: notBefore);

/// Decide where a repeating task lands when it is completed on `completed_on`.
RecurrenceAdvance  advanceOnCompletion({String? recurrence , CivilDate? due , required bool dueHasTime , required CivilDate completedOn }) => RustLib.instance.api.crateApiTasksAdvanceOnCompletion(recurrence: recurrence, due: due, dueHasTime: dueHasTime, completedOn: completedOn);

/// Lay out the task list.
///
/// Subtasks follow their parent and are indented, but only when the parent is
/// on screen too. A subtask whose parent is filtered out — a chore due today
/// under a project scheduled for next week — is promoted to a top-level row
/// rather than vanishing, because a hidden parent must never hide work that is
/// genuinely due.
TaskViewPlan  planTaskView({required List<TaskViewInput> tasks , required TaskViewContext context }) => RustLib.instance.api.crateApiTasksPlanTaskView(tasks: tasks, context: context);

/// Density for every day of one month, so the calendar can show at a glance
/// where the work is. Counts open tasks only; `all_done` marks a day that had
/// tasks and finished them, which is what earns a day its quiet checkmark.
List<CalendarDay>  monthDensity({required List<TaskViewInput> tasks , required int year , required int month , required CivilDate today }) => RustLib.instance.api.crateApiTasksMonthDensity(tasks: tasks, year: year, month: month, today: today);

/// The instant a reminder should fire, as civil components.
///
/// Returns `None` when the task has no due date to hang a reminder on, or when
/// the lead time would place the reminder before the calendar can express it.
/// A lead of zero means "at the due time".
ReminderInstant?  reminderTime({required CivilDate due , CivilTime? dueTime , required int leadMinutes , required int allDayHour , required int allDayMinute }) => RustLib.instance.api.crateApiTasksReminderTime(due: due, dueTime: dueTime, leadMinutes: leadMinutes, allDayHour: allDayHour, allDayMinute: allDayMinute);

List<SortAssignment>  planReorder({required List<String> orderedIds , required List<SortAssignment> current }) => RustLib.instance.api.crateApiTasksPlanReorder(orderedIds: orderedIds, current: current);

            /// One cell of the month strip.
class CalendarDay  {
                final int day;
final int openCount;
final bool hasOverdue;
final bool hasUrgent;
final bool allDone;

                const CalendarDay({required this.day ,required this.openCount ,required this.hasOverdue ,required this.hasUrgent ,required this.allDone ,});





        @override
        int get hashCode => day.hashCode^openCount.hashCode^hasOverdue.hashCode^hasUrgent.hashCode^allDone.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is CalendarDay &&
                runtimeType == other.runtimeType
                && day == other.day&& openCount == other.openCount&& hasOverdue == other.hasOverdue&& hasUrgent == other.hasUrgent&& allDone == other.allDone;

            }

/// A calendar date with no zone attached. Dart localizes it.
class CivilDate  {
                final int year;
final int month;
final int day;

                const CivilDate({required this.year ,required this.month ,required this.day ,});





        @override
        int get hashCode => year.hashCode^month.hashCode^day.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is CivilDate &&
                runtimeType == other.runtimeType
                && year == other.year&& month == other.month&& day == other.day;

            }

/// A wall-clock time with no zone attached.
class CivilTime  {
                final int hour;
final int minute;

                const CivilTime({required this.hour ,required this.minute ,});





        @override
        int get hashCode => hour.hashCode^minute.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is CivilTime &&
                runtimeType == other.runtimeType
                && hour == other.hour&& minute == other.minute;

            }

/// What ticking a repeating task should do.
///
/// `next_due` is `None` for a one-off task or a rule that cannot advance; the
/// caller then completes the task normally. When it is set, the caller instead
/// rolls the task forward and — if `reminder_lead_minutes` was set — moves the
/// reminder with it.
class RecurrenceAdvance  {
                final CivilDate? nextDue;
final bool keepsTime;

                const RecurrenceAdvance({this.nextDue ,required this.keepsTime ,});





        @override
        int get hashCode => nextDue.hashCode^keepsTime.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is RecurrenceAdvance &&
                runtimeType == other.runtimeType
                && nextDue == other.nextDue&& keepsTime == other.keepsTime;

            }

/// How often a task repeats.
enum RecurrenceFreq {
                    daily,
weekly,
monthly,
yearly,
                    ;
                    
                }

/// What the next occurrence is measured from.
///
/// `Schedule` keeps a fixed cadence: rent is due on the 1st whether or not you
/// paid March late. `Completion` measures from the day you finished, which is
/// what "water the plants every 3 days" actually means — the clock restarts
/// when the chore is done, so a late completion pushes the next one out.
enum RecurrenceMode {
                    schedule,
completion,
                    ;
                    
                }

/// A repetition rule.
///
/// `by_weekday` holds Monday-relative indices (0 = Monday). It is only
/// meaningful for [`RecurrenceFreq::Weekly`]; other frequencies ignore it.
class RecurrenceRule  {
                final RecurrenceFreq freq;
final int interval;
final Uint32List byWeekday;
final RecurrenceMode mode;

                const RecurrenceRule({required this.freq ,required this.interval ,required this.byWeekday ,required this.mode ,});





        @override
        int get hashCode => freq.hashCode^interval.hashCode^byWeekday.hashCode^mode.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is RecurrenceRule &&
                runtimeType == other.runtimeType
                && freq == other.freq&& interval == other.interval&& byWeekday == other.byWeekday&& mode == other.mode;

            }

/// When a reminder fires, in civil components Dart pins to an instant.
class ReminderInstant  {
                final CivilDate date;
final CivilTime time;

                const ReminderInstant({required this.date ,required this.time ,});





        @override
        int get hashCode => date.hashCode^time.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is ReminderInstant &&
                runtimeType == other.runtimeType
                && date == other.date&& time == other.time;

            }

/// Renumber a manually reordered list.
///
/// Dart hands over the ids in their new order and gets back only the rows whose
/// rank actually moved, so a drag writes two or three outbox entries instead of
/// rewriting the whole list. Ranks are spaced so the common case — dropping one
/// row between two others — usually changes exactly one number.
class SortAssignment  {
                final String id;
final PlatformInt64 sortOrder;

                const SortAssignment({required this.id ,required this.sortOrder ,});





        @override
        int get hashCode => id.hashCode^sortOrder.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is SortAssignment &&
                runtimeType == other.runtimeType
                && id == other.id&& sortOrder == other.sortOrder;

            }

/// A rendered row. `depth` is the indent level (0 for a top-level task) and the
/// child counts drive the "2/5" progress a parent shows.
class TaskRow  {
                final String id;
final int depth;
final int childTotal;
final int childDone;
final bool isOverdue;

                const TaskRow({required this.id ,required this.depth ,required this.childTotal ,required this.childDone ,required this.isOverdue ,});





        @override
        int get hashCode => id.hashCode^depth.hashCode^childTotal.hashCode^childDone.hashCode^isOverdue.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is TaskRow &&
                runtimeType == other.runtimeType
                && id == other.id&& depth == other.depth&& childTotal == other.childTotal&& childDone == other.childDone&& isOverdue == other.isOverdue;

            }

class TaskSection  {
                final TaskSectionKind kind;
final String label;
final List<TaskRow> rows;

                const TaskSection({required this.kind ,required this.label ,required this.rows ,});





        @override
        int get hashCode => kind.hashCode^label.hashCode^rows.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is TaskSection &&
                runtimeType == other.runtimeType
                && kind == other.kind&& label == other.label&& rows == other.rows;

            }

/// Which band of the list a row belongs to.
enum TaskSectionKind {
                    overdue,
focus,
anytime,
completed,
                    ;
                    
                }

/// How a list is ordered.
enum TaskSort {
                    /// Urgency first, then the clock, then the user's own arrangement.
smart,
/// The user's drag order, untouched.
manual,
priority,
dueDate,
alphabetical,
                    ;
                    
                }

/// What the screen is currently showing.
class TaskViewContext  {
                final CivilDate today;
/// The day the list is focused on. Equal to `today` on open.
final CivilDate focus;
final TaskSort sort;
final bool showCompleted;
/// Whether undated tasks appear. They only make sense on today's list;
/// showing a backlog under a future date implies it is due then.
final bool showAnytime;

                const TaskViewContext({required this.today ,required this.focus ,required this.sort ,required this.showCompleted ,required this.showAnytime ,});





        @override
        int get hashCode => today.hashCode^focus.hashCode^sort.hashCode^showCompleted.hashCode^showAnytime.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is TaskViewContext &&
                runtimeType == other.runtimeType
                && today == other.today&& focus == other.focus&& sort == other.sort&& showCompleted == other.showCompleted&& showAnytime == other.showAnytime;

            }

/// One task, reduced to what ordering and grouping need.
///
/// `due` and `completed_on` are civil dates Dart has already localized.
/// `created_seq` is any stable, monotonically increasing stamp (microseconds
/// since epoch works) used only as the final tiebreak, so ordering is total
/// and two runs over the same data can never disagree.
class TaskViewInput  {
                final String id;
final String title;
final String? parentId;
final int priority;
final CivilDate? due;
final CivilTime? dueTime;
final bool isCompleted;
final CivilDate? completedOn;
final PlatformInt64 sortOrder;
final PlatformInt64 createdSeq;

                const TaskViewInput({required this.id ,required this.title ,this.parentId ,required this.priority ,this.due ,this.dueTime ,required this.isCompleted ,this.completedOn ,required this.sortOrder ,required this.createdSeq ,});





        @override
        int get hashCode => id.hashCode^title.hashCode^parentId.hashCode^priority.hashCode^due.hashCode^dueTime.hashCode^isCompleted.hashCode^completedOn.hashCode^sortOrder.hashCode^createdSeq.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is TaskViewInput &&
                runtimeType == other.runtimeType
                && id == other.id&& title == other.title&& parentId == other.parentId&& priority == other.priority&& due == other.due&& dueTime == other.dueTime&& isCompleted == other.isCompleted&& completedOn == other.completedOn&& sortOrder == other.sortOrder&& createdSeq == other.createdSeq;

            }

/// The whole plan for one screenful, plus the counts the header shows.
class TaskViewPlan  {
                final List<TaskSection> sections;
final int openCount;
final int overdueCount;
final int completedCount;

                const TaskViewPlan({required this.sections ,required this.openCount ,required this.overdueCount ,required this.completedCount ,});





        @override
        int get hashCode => sections.hashCode^openCount.hashCode^overdueCount.hashCode^completedCount.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is TaskViewPlan &&
                runtimeType == other.runtimeType
                && sections == other.sections&& openCount == other.openCount&& overdueCount == other.overdueCount&& completedCount == other.completedCount;

            }
            