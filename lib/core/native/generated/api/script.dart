// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0-beta.5.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'ocr.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `count_letters`, `recognizable`, `total`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `LetterCounts`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`
// These functions are ignored (category: IgnoreBecauseOwnerTyShouldIgnore): `default`

/// Which script some already-recognized text is written in.
ScriptReport detectScript({required String text}) =>
    RustLib.instance.api.crateApiScriptDetectScript(text: text);

/// Judge one recognizer's attempt at a page.
ReadingScore scoreScriptReading({required ScriptReading reading}) =>
    RustLib.instance.api.crateApiScriptScoreScriptReading(reading: reading);

/// Whether a reading looks like the wrong model was used.
///
/// This is the gate on the expensive path: only when the cheap Latin attempt
/// looks wrong is it worth running the other models over the same image.
bool readingLooksWrong({required ReadingScore score}) =>
    RustLib.instance.api.crateApiScriptReadingLooksWrong(score: score);

/// Pick the best of several readings of the same page.
ScriptChoice chooseScriptReading({required List<ScriptReading> readings}) =>
    RustLib.instance.api.crateApiScriptChooseScriptReading(readings: readings);

/// How well a reading came out.
class ReadingScore {
  final TextScript script;

  /// Comparable across readings of the same page. Higher is better.
  final double score;
  final int characters;
  final double meanConfidence;

  /// Share of the page covered by text boxes.
  final double coverage;

  /// Share of characters that are neither letters nor digits — the signature
  /// of a model reading a script it does not know.
  final double junkShare;

  /// The script the recognized text actually turned out to be in, which is
  /// not always the one the model was looking for.
  final TextScript dominantScript;

  const ReadingScore({
    required this.script,
    required this.score,
    required this.characters,
    required this.meanConfidence,
    required this.coverage,
    required this.junkShare,
    required this.dominantScript,
  });

  @override
  int get hashCode =>
      script.hashCode ^
      score.hashCode ^
      characters.hashCode ^
      meanConfidence.hashCode ^
      coverage.hashCode ^
      junkShare.hashCode ^
      dominantScript.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ReadingScore &&
          runtimeType == other.runtimeType &&
          script == other.script &&
          score == other.score &&
          characters == other.characters &&
          meanConfidence == other.meanConfidence &&
          coverage == other.coverage &&
          junkShare == other.junkShare &&
          dominantScript == other.dominantScript;
}

/// Which reading won, and why.
class ScriptChoice {
  /// Index into the readings that were offered. -1 when none were usable.
  final int chosen;
  final TextScript script;
  final List<ReadingScore> scores;
  final String reason;

  const ScriptChoice({
    required this.chosen,
    required this.script,
    required this.scores,
    required this.reason,
  });

  @override
  int get hashCode =>
      chosen.hashCode ^ script.hashCode ^ scores.hashCode ^ reason.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ScriptChoice &&
          runtimeType == other.runtimeType &&
          chosen == other.chosen &&
          script == other.script &&
          scores == other.scores &&
          reason == other.reason;
}

/// One recognizer's attempt at a page.
class ScriptReading {
  /// The script the model that produced this was looking for.
  final TextScript script;
  final OcrPageInput page;

  const ScriptReading({required this.script, required this.page});

  @override
  int get hashCode => script.hashCode ^ page.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ScriptReading &&
          runtimeType == other.runtimeType &&
          script == other.script &&
          page == other.page;
}

/// What script a piece of text is written in.
class ScriptReport {
  final TextScript script;

  /// Share of the letters that belong to [script], 0..1.
  final double confidence;

  /// Letters counted, ignoring digits, spaces and punctuation.
  final int letters;

  /// Whether an on-device recognizer exists for [script].
  final bool recognizable;

  const ScriptReport({
    required this.script,
    required this.confidence,
    required this.letters,
    required this.recognizable,
  });

  @override
  int get hashCode =>
      script.hashCode ^
      confidence.hashCode ^
      letters.hashCode ^
      recognizable.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ScriptReport &&
          runtimeType == other.runtimeType &&
          script == other.script &&
          confidence == other.confidence &&
          letters == other.letters &&
          recognizable == other.recognizable;
}

/// A writing system, as far as picking a recognizer is concerned.
enum TextScript {
  unknown,
  latin,
  chinese,
  japanese,
  korean,
  devanagari,

  /// Detectable, but no on-device model reads it.
  cyrillic,
  greek,
  arabic,
  hebrew,
  thai,
}
