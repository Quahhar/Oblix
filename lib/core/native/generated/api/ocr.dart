// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0-beta.5.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';


            // These functions are ignored because they are not marked as `pub`: `alignment_score`, `assign_bands`, `build_rows`, `centre_x`, `centre_y`, `classify`, `column_of`, `confidence_stats`, `count`, `detect_columns_in`, `digit_share`, `drop_specks`, `edge_positions`, `empty_draft`, `estimate_skew`, `estimate_tilt_sine`, `extract_title`, `filter_lines`, `indent_levels`, `input_centre_x`, `input_centre_y`, `label`, `looks_labelled`, `measure_page_geometry`, `median`, `place`, `reconstruct`, `render_options`, `row_bucket`, `running_key`, `strip_running_heads`, `symbol_share`, `tidy`, `truncate_title`, `undo_tilt_inflation`, `width`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `PageGeometry`, `Placed`, `Reconstruction`, `RowCell`, `Row`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`


            /// Rebuild note text from one page of recognized line boxes.
ScannedNoteDraft  shapeScannedText({required List<OcrLineInput> lines , required OcrShapeOptions options }) => RustLib.instance.api.crateApiOcrShapeScannedText(lines: lines, options: options);

/// Rebuild note text from a whole capture.
///
/// Pages are reconstructed one at a time — tilt and column structure are
/// properties of a photograph, not of the document — and only then considered
/// together, which is what makes running heads and paragraphs broken over a
/// page break visible at all.
ScannedNoteDraft  shapeScannedPages({required List<OcrPageInput> pages , required OcrShapeOptions options }) => RustLib.instance.api.crateApiOcrShapeScannedPages(pages: pages, options: options);

            /// What the capture looked like to the recognizer.
class CaptureQuality  {
                final QualityVerdict verdict;
/// Mean reported confidence across scored lines, or 0 when none were.
final double meanConfidence;
/// Share of scored lines below [LOW_CONFIDENCE].
final double lowConfidenceShare;
/// Lines the recognizer scored at all.
final int scoredLines;
/// Empty when there is nothing worth saying.
final String advice;

                const CaptureQuality({required this.verdict ,required this.meanConfidence ,required this.lowConfidenceShare ,required this.scoredLines ,required this.advice ,});





        @override
        int get hashCode => verdict.hashCode^meanConfidence.hashCode^lowConfidenceShare.hashCode^scoredLines.hashCode^advice.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is CaptureQuality &&
                runtimeType == other.runtimeType
                && verdict == other.verdict&& meanConfidence == other.meanConfidence&& lowConfidenceShare == other.lowConfidenceShare&& scoredLines == other.scoredLines&& advice == other.advice;

            }

/// One recognized line and where it sat on the page.
class OcrLineInput  {
                final String text;
final double left;
final double top;
final double right;
final double bottom;
/// The recognizer's own grouping, kept so a caller can pass it through;
/// reconstruction relies on geometry instead, because block grouping is
/// unreliable on photographs.
final int blockIndex;
/// 0..1 where the recognizer reports it. Absent means "no opinion".
final double? confidence;

                const OcrLineInput({required this.text ,required this.left ,required this.top ,required this.right ,required this.bottom ,required this.blockIndex ,this.confidence ,});





        @override
        int get hashCode => text.hashCode^left.hashCode^top.hashCode^right.hashCode^bottom.hashCode^blockIndex.hashCode^confidence.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is OcrLineInput &&
                runtimeType == other.runtimeType
                && text == other.text&& left == other.left&& top == other.top&& right == other.right&& bottom == other.bottom&& blockIndex == other.blockIndex&& confidence == other.confidence;

            }

/// One page's worth of recognized lines.
class OcrPageInput  {
                final List<OcrLineInput> lines;
/// Source pixel size. Zero means "unknown" — used only for the heuristics
/// that classify a page, never for reconstruction, which works off the
/// text block's own extent.
final double width;
final double height;

                const OcrPageInput({required this.lines ,required this.width ,required this.height ,});





        @override
        int get hashCode => lines.hashCode^width.hashCode^height.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is OcrPageInput &&
                runtimeType == other.runtimeType
                && lines == other.lines&& width == other.width&& height == other.height;

            }

/// Tunables the caller may want to expose in the UI.
class OcrShapeOptions  {
                /// Lines scoring below this are dropped. Lines without a score are kept.
final double minConfidence;
/// Keep every recognized line as its own line instead of reflowing
/// wrapped lines back into paragraphs. Useful for receipts and code.
final bool preserveLineBreaks;
/// Look for columns and read them one at a time. Off means straight
/// top-to-bottom, which is what a receipt or a form wants.
final bool detectColumns;
/// Read the page's structure — headings, lists, quotes — and write it out
/// as Markdown. When nothing structural is found the body stays plain.
final bool detectStructure;
/// Recognize aligned rows as a table and draw it as a Markdown table.
final bool detectTables;
/// Drop lines that repeat in the same place on most pages.
final bool stripRunningHeads;
/// Rejoin a paragraph that was cut in half by a page break.
final bool healAcrossPages;
/// How to read the page. [`ScanPreset::Auto`] classifies it, and the
/// chosen preset then overrides the flags above that it has an opinion on.
final ScanPreset preset;

                const OcrShapeOptions({required this.minConfidence ,required this.preserveLineBreaks ,required this.detectColumns ,required this.detectStructure ,required this.detectTables ,required this.stripRunningHeads ,required this.healAcrossPages ,required this.preset ,});

                static Future<OcrShapeOptions>  default_()=>RustLib.instance.api.crateApiOcrOcrShapeOptionsDefault();





        @override
        int get hashCode => minConfidence.hashCode^preserveLineBreaks.hashCode^detectColumns.hashCode^detectStructure.hashCode^detectTables.hashCode^stripRunningHeads.hashCode^healAcrossPages.hashCode^preset.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is OcrShapeOptions &&
                runtimeType == other.runtimeType
                && minConfidence == other.minConfidence&& preserveLineBreaks == other.preserveLineBreaks&& detectColumns == other.detectColumns&& detectStructure == other.detectStructure&& detectTables == other.detectTables&& stripRunningHeads == other.stripRunningHeads&& healAcrossPages == other.healAcrossPages&& preset == other.preset;

            }

/// How much the recognizer struggled, which is a decent proxy for whether the
/// photograph was any good.
enum QualityVerdict {
                    /// The recognizer reported no confidences at all, so there is nothing to
/// judge. Not the same as "good".
unknown,
good,
fair,
poor,
                    ;
                    
                }

/// How a page should be read.
///
/// [`ScanPreset::Auto`] picks one from the page's own geometry; the rest force
/// a choice when the caller knows better than the classifier does.
enum ScanPreset {
                    auto,
/// Ordinary flowing text. Wrapped lines rejoin into paragraphs.
prose,
/// A page from a book: prose, plus running heads and page numbers removed
/// and paragraphs healed across the page break.
bookPage,
/// Receipts and tickets: every line kept, columns off, tables off.
receipt,
/// A form: label and value pairs, read straight down.
form,
/// A whiteboard or sticky notes: keep the clusters, never reflow.
whiteboard,
/// Source code: preserve every break and indent, fence the result.
code,
/// A page that is mostly one big table.
table,
                    ;
                    
                }

/// A note ready to be created, plus what the reconstruction made of the pages.
class ScannedNoteDraft  {
                final String title;
final String body;
/// `plain` or `markdown`, matching the note content types. Markdown only
/// when structure was actually found, so a plain page stays plain.
final String contentType;
/// Lines that survived filtering and made it into [body].
final int keptLines;
/// Lines dropped as low-confidence, empty, or too small to be text.
final int droppedLines;
/// Columns of the widest page. 1 for ordinary prose.
final int columns;
/// Page tilt that was corrected, in degrees, worst page first. Negative
/// leans left.
final double correctedSkewDegrees;
final int pages;
/// Tables drawn into [body].
final int tables;
/// Headings found and written as Markdown headings.
final int headings;
/// Lines removed as running heads, feet, or page numbers.
final int strippedRunningHeads;
/// The preset actually used, never `auto`.
final String preset;
final CaptureQuality quality;

                const ScannedNoteDraft({required this.title ,required this.body ,required this.contentType ,required this.keptLines ,required this.droppedLines ,required this.columns ,required this.correctedSkewDegrees ,required this.pages ,required this.tables ,required this.headings ,required this.strippedRunningHeads ,required this.preset ,required this.quality ,});





        @override
        int get hashCode => title.hashCode^body.hashCode^contentType.hashCode^keptLines.hashCode^droppedLines.hashCode^columns.hashCode^correctedSkewDegrees.hashCode^pages.hashCode^tables.hashCode^headings.hashCode^strippedRunningHeads.hashCode^preset.hashCode^quality.hashCode;



        @override
        bool operator ==(Object other) =>
            identical(this, other) ||
            other is ScannedNoteDraft &&
                runtimeType == other.runtimeType
                && title == other.title&& body == other.body&& contentType == other.contentType&& keptLines == other.keptLines&& droppedLines == other.droppedLines&& columns == other.columns&& correctedSkewDegrees == other.correctedSkewDegrees&& pages == other.pages&& tables == other.tables&& headings == other.headings&& strippedRunningHeads == other.strippedRunningHeads&& preset == other.preset&& quality == other.quality;

            }
            