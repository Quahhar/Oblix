// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0-beta.5.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'textlayer.dart';

// These functions are ignored because they are not marked as `pub`: `collect_dates`, `days_in_month`, `expand_year`, `first_line`, `group_number`, `iso_or_empty`, `label_for`, `luhn`, `month_number`, `normalize_time`, `parse_money`, `patterns`, `priority`, `resolve`, `sensitive`, `utf16_offset`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `Patterns`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `eq`, `eq`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`

/// Find every entity in the text, without overlaps.
List<Entity> extractEntities({
  required String text,
  required EntityOptions options,
}) => RustLib.instance.api.crateApiEntitiesExtractEntities(
  text: text,
  options: options,
);

/// Boxes over everything worth hiding on a scanned page.
///
/// `kinds` empty means every sensitive kind — cards, bank numbers, telephone
/// numbers and email addresses. Boxes are narrowed to the matched characters
/// the same way a search hit is, so redacting a card number on a line does not
/// black out the whole line.
List<RedactionSpan> findRedactions({
  required TextLayer layer,
  required List<EntityKind> kinds,
  required EntityOptions options,
}) => RustLib.instance.api.crateApiEntitiesFindRedactions(
  layer: layer,
  kinds: kinds,
  options: options,
);

/// What the app could offer to do with this scan.
///
/// Kept conservative on purpose: a page full of numbers should not produce a
/// page full of suggestions. A total is only proposed when a line says it is
/// one, and a date only when the text gave a year to hang it on.
List<SuggestedAction> suggestActions({
  required String text,
  required EntityOptions options,
}) => RustLib.instance.api.crateApiEntitiesSuggestActions(
  text: text,
  options: options,
);

/// Something the app could offer to do with the scan.
enum ActionKind { task, event, contact }

/// One thing found in the text.
class Entity {
  final EntityKind kind;

  /// Exactly the characters that matched.
  final String text;

  /// UTF-16 offsets, so they index the same string Dart holds.
  final int start;
  final int end;

  /// A canonical form where one exists and is unambiguous: `YYYY-MM-DD` for
  /// a date, `HH:MM` for a time, digits for a phone number, the amount in
  /// minor units for money. Empty when the match cannot be normalized
  /// without guessing.
  final String normalized;

  /// ISO currency code for [EntityKind::Money], else empty.
  final String currency;

  const Entity({
    required this.kind,
    required this.text,
    required this.start,
    required this.end,
    required this.normalized,
    required this.currency,
  });

  @override
  int get hashCode =>
      kind.hashCode ^
      text.hashCode ^
      start.hashCode ^
      end.hashCode ^
      normalized.hashCode ^
      currency.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is Entity &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          text == other.text &&
          start == other.start &&
          end == other.end &&
          normalized == other.normalized &&
          currency == other.currency;
}

/// What was found.
enum EntityKind {
  date,
  time,
  money,
  percent,
  phone,
  email,
  url,

  /// A payment card number that passes a Luhn check.
  card,
  iban,
  postCode,
}

/// What the caller knows that the text does not.
class EntityOptions {
  /// Whether `01/02/2026` means the first of February. Set from the device
  /// locale; when the day exceeds twelve the text settles it either way.
  final bool dayFirst;

  const EntityOptions({required this.dayFirst});

  static Future<EntityOptions> default_() =>
      RustLib.instance.api.crateApiEntitiesEntityOptionsDefault();

  @override
  int get hashCode => dayFirst.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is EntityOptions &&
          runtimeType == other.runtimeType &&
          dayFirst == other.dayFirst;
}

/// A rectangle on a page to paint over.
class RedactionSpan {
  final EntityKind kind;
  final int page;
  final double left;
  final double top;
  final double right;
  final double bottom;

  /// What is being hidden, so the UI can say so without showing it.
  final String label;

  const RedactionSpan({
    required this.kind,
    required this.page,
    required this.left,
    required this.top,
    required this.right,
    required this.bottom,
    required this.label,
  });

  @override
  int get hashCode =>
      kind.hashCode ^
      page.hashCode ^
      left.hashCode ^
      top.hashCode ^
      right.hashCode ^
      bottom.hashCode ^
      label.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is RedactionSpan &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          page == other.page &&
          left == other.left &&
          top == other.top &&
          right == other.right &&
          bottom == other.bottom &&
          label == other.label;
}

/// A proposed action, in civil components. Dart resolves these against the
/// device timezone; the core never does.
class SuggestedAction {
  final ActionKind kind;
  final String title;
  final String detail;
  final int? year;
  final int? month;
  final int? day;
  final int? hour;
  final int? minute;

  /// Money in minor units — pence, cents — so nothing is lost to rounding.
  final PlatformInt64? amountMinor;
  final String currency;

  const SuggestedAction({
    required this.kind,
    required this.title,
    required this.detail,
    this.year,
    this.month,
    this.day,
    this.hour,
    this.minute,
    this.amountMinor,
    required this.currency,
  });

  @override
  int get hashCode =>
      kind.hashCode ^
      title.hashCode ^
      detail.hashCode ^
      year.hashCode ^
      month.hashCode ^
      day.hashCode ^
      hour.hashCode ^
      minute.hashCode ^
      amountMinor.hashCode ^
      currency.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SuggestedAction &&
          runtimeType == other.runtimeType &&
          kind == other.kind &&
          title == other.title &&
          detail == other.detail &&
          year == other.year &&
          month == other.month &&
          day == other.day &&
          hour == other.hour &&
          minute == other.minute &&
          amountMinor == other.amountMinor &&
          currency == other.currency;
}
